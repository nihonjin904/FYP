---
plan: 04-01
phase: 4
title: "Merge feature/perilous-attack-notify 去 master"
wave: 1
depends_on: []
autonomous: false
files_modified:
  - Source/FYP/Public/UI/SekiroHUD.h
  - Source/FYP/Private/UI/SekiroHUD.cpp
  - Source/FYP/Private/Core/SekiroGameMode.cpp
  - Content/ABP_SekiroEnemy_New.uasset
  - Content/boss_anim_retarget/AM_PerilousAttack_Slash.uasset
  - Content/boss_anim_retarget/AM_PerilousAttack_Sweep.uasset
  - Content/boss_anim_retarget/AM_PerilousAttack_Thrust.uasset
  - Content/boss_anim_retarget/AM_Perilous_Slash_Patchouli.uasset
  - Content/boss_anim_retarget/AM_Perilous_Thrust_Patchouli.uasset
requirements: []
---

# Plan 04-01: Merge feature/perilous-attack-notify 去 master

**目標：** 把危攻擊功能（UI、傷害、Posture）安全合併進 master，保留朋友的地圖/Fast Travel 改動。

**執行方式：** 手動（autonomous: false）→ 你用 GitHub Desktop + VS Code 操作

---

## Pre-Condition Check

<task id="04-01-A" name="確認雙方 branch 都乾淨">
<read_first>
- 看 GitHub Desktop 左側 Changes tab → 必須顯示 "0 changed files"
- 看頂部 "Fetch origin" → 按一下確保 push/pull 最新
</read_first>
<action>
1. 在 GitHub Desktop 頂部右側按 **"Fetch origin"**（或 Ctrl+Shift+F）
2. 確認左側 "Changes" tab 顯示 **0 changed files**
3. 如果有未 commit 的文件 → 先 commit 或 discard 它
</action>
<acceptance_criteria>
- GitHub Desktop 左側顯示 "0 changed files"
- Fetch origin 完成，無錯誤
</acceptance_criteria>
</task>

---

## Step 1: 切換到 master branch

<task id="04-01-B" name="在 GitHub Desktop 切換到 master">
<read_first>
- GitHub Desktop 頂部中間顯示 "Current branch: feature/perilous-attack-notify"
</read_first>
<action>
1. 點擊 GitHub Desktop 頂部中間的 **"Current branch: feature/perilous-attack-n..."** 按鈕
2. 在下拉清單 "Recent branches" 中，點擊 **"master"**（3 hours ago 那一行）
3. 等待 GitHub Desktop 切換完成
4. 確認頂部顯示 **"Current branch: master"**
</action>
<acceptance_criteria>
- 頂部 "Current branch" 顯示 **master**
- 左側 Changes 仍然顯示 0 changed files（切換後 master 是乾淨的）
</acceptance_criteria>
</task>

---

## Step 2: 執行 Merge

<task id="04-01-C" name="在 GitHub Desktop merge feature branch 進 master">
<read_first>
- 確認頂部顯示 "Current branch: master"（你已在 master）
</read_first>
<action>
1. 在 GitHub Desktop 頂部選單 → 點擊 **"Branch"**
2. 點擊 **"Merge into current branch..."**（快捷鍵 Ctrl+Shift+M）
3. 在彈出的對話框，**"Recent branches"** 下選擇 **"feature/perilous-attack-notify"**
4. 底部確認顯示 **"This will merge feature/perilous-attack-notify into master"**
5. 按 **"Create a merge commit"** 藍色按鈕

**⚠️ 注意：如果按鈕顯示紅色或灰色，有衝突 → 繼續 Step 3**
**✅ 如果按鈕正常可按且沒有衝突提示 → Merge 成功，跳去 Step 5**
</action>
<acceptance_criteria>
- 對話框底部顯示 "This will merge feature/perilous-attack-notify into master"（而非反向）
- 沒有 "Unable to merge unrelated histories" 錯誤（這錯誤只在選 "main" 時出現，選 master 沒有）
</acceptance_criteria>
</task>

---

## Step 3: 處理文字衝突（如有）

**只有在 GitHub Desktop 說 "Conflicts" 時才做這步。**

<task id="04-01-D" name="解決 SekiroHUD.h 衝突">
<read_first>
- GitHub Desktop 會標紅衝突文件
- Source/FYP/Public/UI/SekiroHUD.h
</read_first>
<action>
1. GitHub Desktop 右鍵衝突文件 → "Open in Visual Studio Code"
2. VS Code 中找 `<<<<<<<`、`=======`、`>>>>>>>` 衝突標記
3. **SekiroHUD.h 解決規則：兩邊都保留**

   衝突區域長這樣：
   ```
   <<<<<<< HEAD（master 朋友的版本）
   // 朋友加的 map 相關聲明
   =======
   // 你加的危攻擊相關聲明
   >>>>>>> feature/perilous-attack-notify
   ```

   👉 **做法：刪掉 `<<<<<<<`、`=======`、`>>>>>>>` 三行，保留兩邊的 code，拼在一起**

4. 確保以下你加的聲明都在：
   - `ASekiroHUD();`（Constructor）
   - `TSubclassOf<class UUserWidget> PerilousWarningWidgetClass;`
   - `TObjectPtr<class UUserWidget> PerilousWarningWidgetInstance;`

5. 儲存文件（Ctrl+S）
</action>
<acceptance_criteria>
- SekiroHUD.h 不含任何 `<<<<<<<`、`=======`、`>>>>>>>` 標記
- 文件包含 `ASekiroHUD();` 聲明
- 文件包含 `PerilousWarningWidgetClass` 聲明
</acceptance_criteria>
</task>

<task id="04-01-E" name="解決 SekiroHUD.cpp 衝突">
<read_first>
- Source/FYP/Private/UI/SekiroHUD.cpp
</read_first>
<action>
1. VS Code 打開 SekiroHUD.cpp
2. 找所有 `<<<<<<<`、`=======`、`>>>>>>>` 標記
3. **SekiroHUD.cpp 解決規則：兩邊都保留**

   例子：
   ```cpp
   <<<<<<< HEAD
   // 朋友加的 Map UI 函數
   void ASekiroHUD::ToggleMapUI() { ... }
   =======
   // 你加的危攻擊 Constructor
   ASekiroHUD::ASekiroHUD() { ... }
   >>>>>>> feature/perilous-attack-notify
   ```

   👉 **做法：刪掉三行標記，兩個函數都保留**

4. 確保 `OnPerilousAttackStarted()` 函數完整存在：
   ```cpp
   void ASekiroHUD::OnPerilousAttackStarted()
   {
       if (PerilousWarningWidgetClass && GetOwningPlayerController())
       { ... }
       bShowPerilousWarning = true;
       PerilousWarningTimer = PerilousWarningDuration;
   }
   ```

5. 儲存文件（Ctrl+S）
</action>
<acceptance_criteria>
- SekiroHUD.cpp 不含任何衝突標記
- 文件包含 `ASekiroHUD::ASekiroHUD()` Constructor
- 文件包含完整 `OnPerilousAttackStarted()` 實現
</acceptance_criteria>
</task>

<task id="04-01-F" name="解決 SekiroGameMode.cpp 衝突（如有）">
<read_first>
- Source/FYP/Private/Core/SekiroGameMode.cpp
</read_first>
<action>
1. VS Code 打開 SekiroGameMode.cpp
2. 找衝突標記（如果沒有則跳過此步）
3. 保留規則：
   - `#include "UI/SekiroHUD.h"` → **保留（你加的）**
   - `HUDClass = ASekiroHUD::StaticClass();` → **保留（你加的）**
   - 朋友加的任何 GameMode 設定 → **也保留**
4. 儲存文件
</action>
<acceptance_criteria>
- SekiroGameMode.cpp 不含衝突標記
- 文件包含 `#include "UI/SekiroHUD.h"`
- 文件包含 `HUDClass = ASekiroHUD::StaticClass();`
</acceptance_criteria>
</task>

<task id="04-01-G" name="在 GitHub Desktop 標記衝突已解決，完成 merge">
<read_first>
- 所有衝突文件已在 VS Code 儲存，無衝突標記
</read_first>
<action>
1. 回到 GitHub Desktop
2. 所有衝突文件旁邊的紅色警告應已消失，變為可 commit
3. 左下角填入 commit message：`Merge feature/perilous-attack-notify into master`
4. 按 **"Commit merge"** 按鈕
5. 按 **"Push origin"** 把 master push 去 GitHub
</action>
<acceptance_criteria>
- GitHub Desktop 左側顯示 merge commit 已完成
- "Push origin" 成功，GitHub 上 master 包含 merge commit
</acceptance_criteria>
</task>

---

## Step 4: .uasset 衝突（如有）

<task id="04-01-H" name="處理 Blueprint .uasset 衝突（如有）">
<read_first>
- 這步只在 GitHub Desktop 標記 .uasset 有衝突時才執行
- 預測機率：極低（你的 .uasset 是 Combat，朋友是 Map）
</read_first>
<action>
**如果 GitHub Desktop 標記以下文件有衝突：**
- ABP_SekiroEnemy_New.uasset
- AM_PerilousAttack_*.uasset

**做法（.uasset 是二進位，不能手動合併）：**
1. GitHub Desktop 右鍵衝突的 .uasset 文件
2. 選 **"Use the modified file from feature/perilous-attack-notify"**（選你的版本）
3. 告知朋友哪些文件選了你的版本，讓他確認他的改動是否在其他文件

**如果是地圖/UI 相關 .uasset 衝突（朋友的）：**
1. 右鍵 → **"Use the modified file from master"**（選朋友的版本）
</action>
<acceptance_criteria>
- 所有 .uasset 衝突都選好了版本（沒有未解決的衝突）
- GitHub Desktop 無紅色衝突標記
</acceptance_criteria>
</task>

---

## Step 5: Rebuild C++ 並驗證

<task id="04-01-I" name="Full Rebuild 確保 Merge 後 C++ 編譯成功">
<read_first>
- Merge 後 SekiroHUD.h 有新 UPROPERTY → 必須 Full Rebuild（不能 Live Coding）
- 參考 bullshit.md 記錄的 UPROPERTY Live Coding crash 規則
</read_first>
<action>
1. **關閉 UE5 Editor**（如果開著）
2. 打開 **Visual Studio**（.sln 文件）
3. 頂部選單 → **Build** → **Rebuild Solution**（不是 Build，是 Rebuild）
4. 等待 Rebuild 完成（約 2-5 分鐘）
5. 確認輸出視窗顯示 `Rebuild: 1 succeeded, 0 failed`
6. 重新打開 UE5 Editor
</action>
<acceptance_criteria>
- Visual Studio Rebuild 輸出：`Rebuild: 1 succeeded, 0 failed`
- UE5 Editor 可以正常打開，無 crash
- UE5 Output Log 無 C++ compile error
</acceptance_criteria>
</task>

---

## must_haves（Merge 成功的必要條件）

- [ ] master branch 包含 `ASekiroHUD::ASekiroHUD()` Constructor
- [ ] master branch 包含 `OnPerilousAttackStarted()` 危攻擊 Widget 顯示邏輯
- [ ] master branch 包含 `HUDClass = ASekiroHUD::StaticClass()` 在 GameMode
- [ ] Visual Studio Rebuild 成功（0 failed）
- [ ] GitHub master push 成功
- [ ] .uasset 危攻擊 Montage AnimNotify 存在（在 UE5 Editor 確認）

---

## Verification（你手動 UAT 測試）

打開 UE5 → Play mode → 驗證以下：

| 測試項目 | 如何測試 | 預期結果 |
|---|---|---|
| ① 危攻擊 UI | Boss 發動危攻擊 | 「避」字出現，約 1 秒後消失 |
| ② 危攻擊傷害 | 玩家格擋狀態被打 | 仍然受傷（無法 block）|
| ③ 架勢條 | 精準擋刀後 | 固定增量，不瞬間爆滿 |
| ④ 地圖（朋友）| 按地圖鍵 | 全屏地圖正常顯示 |
| ⑤ Fast Travel（朋友）| 快速傳送 | 正常傳送到目的地 |

---

## 緊急回退（萬一出錯）

```bash
# Merge 後發現問題：撤銷 merge commit（local）
git reset --hard HEAD~1

# 已 push 後發現問題：revert merge commit
git revert -m 1 HEAD
git push origin master
```

或者直接告訴我，我幫你用 git reflog 找回任何狀態。
